# impact-numbers

## Local Setup
pip install -r requirements.txt

Add new Directory to Reciepts for current month

Add Reciepts

run main.py

go to http://127.0.0.1:500 to see changes (script may take some time to finish)
